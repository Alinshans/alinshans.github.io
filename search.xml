<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title><![CDATA[OpenCV 笔记（二）：Mat 类初探]]></title>
    <url>%2F2017%2F07%2F09%2Fp1707091%2F</url>
    <content type="text"><![CDATA[在 OpenCV 的很多跟图像有关的东西里，都离不开一样东西 —— Mat 类。读一张图片，返回的是一个 Mat，显示一张图片，用的也是 Mat。我们都知道这是一个矩阵类，但它到底是如何工作的呢？如何将一个矩阵跟各种图像、图像间关系、图像间变换等东西联系起来呢？其实我们多多少少都有一点了解，但概念又不是特别清晰。所以十分有必要去深入的探索一下这个 Mat 类，我们才能更有效且高效地写出想要的程序。正如我们要对 STL 有深入的理解才能更有效且高效的写出我们想要的程序。 Mat 类的结构这里我们主要讲的是 Mat 类的储存结构，即用什么数据来表现一个 Mat。我们可以通过源码看到它的一些关键变量：1234567891011121314151617181920212223242526272829303132class CV_EXPORTS Mat&#123;public: // 一堆函数 // ... // 以下是主要成员变量 // [1] /*! includes several bit-fields: - the magic signature - continuity flag - depth - number of channels */ int flags; //! the matrix dimensionality, &gt;= 2 int dims; //! the number of rows and columns or (-1, -1) when the matrix has more than 2 dimensions int rows, cols; //! pointer to the data uchar* data; // [2] //! helper fields used in locateROI and adjustROI const uchar* datastart; const uchar* dataend; const uchar* datalimit; // [3] //! interaction with UMat UMatData* u;&#125; 这些我只列出了主要的一些变量，并且分成三个部分： 第[1]部分是与矩阵相关的一些信息，flags 储存了矩阵的标识、是否连续、深度、通道数等信息，dims 代表矩阵的维数，rows 和 cols 代表矩阵的行数与列数，data 是指向储存矩阵数据的指针。 第[2]部分是与感兴趣区域有关的信息，感兴趣区域即 ROI(Region Of Interset)。 第[3]部分是一个指针，指向一个 UMatData 类型，我们查看一下 UMatData 的定义，可以知道这是跟引用计数相关的。 UMatData 的定义：12345678910111213141516171819202122232425struct CV_EXPORTS UMatData&#123; // ... // provide atomic access to the structure void lock(); void unlock(); // 一些函数... const MatAllocator* prevAllocator; const MatAllocator* currAllocator; int urefcount; int refcount; uchar* data; uchar* origdata; size_t size; int flags; void* handle; void* userdata; int allocatorFlags_; int mapcount; UMatData* originalUMatData;&#125;; 这是很容易理解的：由一张图片储存成的矩阵通常不小，例如一张 1024 * 768 的三通道彩色图，储存的矩阵就占了 1024 * 768 * 3 * 1 = 2359296 Byte = 2304 KB。而 OpenCV 作为计算机视觉库，它就是负责处理一大堆的这些图像信息，所以经常拷贝大的图像，开销是非常大的。自然地，就采用了引用计数。一个 Mat 的数据主要包含两个部分：矩阵头和指向像素数据的指针，矩阵头主要包含了：矩阵尺寸、存储方式、存储地址、引用计数等。每个 Mat 类，都会有自己的矩阵头，同时，要是通过引用计数，就可以共享同一片矩阵区域，而不用复制两片一模一样的空间啦！ 图像的储存方式一般来说，一个 M x N 的图像可以由一个 M x N 的矩阵来表示。矩阵如何储存这些像素值呢？需要指定颜色空间和数据类型。颜色空间就是对指定颜色的一种编码，比如最简单的灰度空间，只有黑色跟白色，以及他们的组合可以组成不同程度的灰色。还有常见的彩色空间，可能有三种或四种的基本元素，然后由这些基本元素可以组成各种各样的颜色。RGB颜色空间就是最常用的彩色空间，若再加入第四个元素 Alpha，就可以用来表示透明度。而数据类型就是指组成颜色的这些元素的大小，最小的数据类型就是 char，占一个字节，如 RGB 颜色的元素常用 unsigned char 来表示，可以表示范围 0 ~ 255 的数值。当然还可以用更大的数据类型来获得更精确的颜色分辨。 例如，一个灰度图可由一个二维矩阵表示： P(0,0) P(0,1) … P(0,N-1) P(1,0) P(1,1) … P(1,N-1) … … … … P(M-1,0) P(M-1,1) … P(M-1,N-1) P(i,j) 就表示第 i 行 j 列的像素值。如果是RBG图像，则每个元素用三个字节表示，在 OpenCV 中，RGB 的储存顺序为 BGR ： B(0,0) G(0,0) R(0,0) … B(0,N-1) G(0,N-1) R(0,N-1) B(1,0) G(1,0) R(1,0) … B(1,N-1) G(1,N-1) R(1,N-1) … … … … … … … B(M-1,0) G(M-1,0) R(M-1,0) … B(M-1,N-1) G(M-1,N-1) R(M-1,N-1) Mat 对象的创建OpenCV 为创建一个 Mat 类提供了很多方法，可以在 这里 看到完整的构造的方法，我们列出一些常用的：12345678Mat ()Mat (int rows, int cols, int type)Mat (Size size, int type)Mat (int rows, int cols, int type, const Scalar &amp;s)Mat (Size size, int type, const Scalar &amp;s)Mat (int ndims, const int *sizes, int type)Mat (int ndims, const int *sizes, int type, const Scalar &amp;s)Mat (const Mat &amp;m) 创建空矩阵对于默认构造函数，会创建一个矩阵头，它没有像素数据，所以如果你写出下面这样的代码：12Mat nul;imshow("nul", nul); // running time error 是会产生一个运行时的断言错误的，因为 imshow 要求矩阵的维数必须是二维的（构造时如果构造的是一维的，它也会自动扩展为二维的，让第二个维度的大小为 1）。 创建二维矩阵对于二维的矩阵，就是要指定矩阵的行列以及矩阵数据的类型，初始值是可选的。矩阵类型的定义如下：1CV_[The number of bits per item][Signed or Unsigned][Type Prefix]C[The channel number] 其中，CV_[The number of bits per item][Signed or Unsigned][Type Prefix] 这个部分可以使用以下几个值之一： CV_8U CV_8S CV_16U CV_16S CV_32S CV_32F CV_64F 而预定义的 [The channel number] 可以是 1、2、3、4，当然啦，如果你需要更多的通道数，也可以使用宏 XXX(n) 来生成：1234567CV_8UC(n) // n 通道的、无符号的、每个元素占 8 个 bitCV_8SC(n) // n 通道的、有符号的、每个元素占 8 个 bitCV_16UC(n) // n 通道的、无符号的、每个元素占 16 个 bitCV_16SC(n) // n 通道的、有符号的、每个元素占 16 个 bitCV_32SC(n) // n 通道的、有符号的、每个元素占 32 个 bitCV_32FC(n) // n 通道的、浮点类型、每个元素占 32 个 bitCV_64FC(n) // n 通道的、浮点类型、每个元素占 64 个 bit 比如常用的 RGB 颜色类型是 CV_8UC3，表示每个像素由三个通道组成，而每个元素占 8 个 bit。我们可以用 cv::Scalar 来指定矩阵的初值，比如 Scalar(0) 是一个单通道的黑色的像素，Scalar(0,0,255) 是一个三通道的红色的像素。于是，我们就可以用下面的代码创建一张全红的图片：12Mat red(200, 200, CV_8UC3, Scalar(0,0,255));imshow("red", red); 效果： 创建多维矩阵如果你需要更高维数的矩阵，它也提供了相应的构造函数，首先要指定维数，然后需要一个一维数组的指针，数组里包含了各个维度的尺寸，以及矩阵数据的类型，初始值也是可选的。例如：12int m[3] = &#123; 10,10,10 &#125;;Mat mat(3, m, CV_8UC1, Scalar(0)); 以上代码创建了一个 10 x 10 x 10 的三维矩阵，你仍然可以对它做矩阵运算，但是不能使用诸如 imshow 等函数了，因为必须的二维的图像才能显示出来 ^_^在 这里 的 Detailed Description 有一句话： It passes the number of dimensions =1 to the Mat constructor but the created array will be 2-dimensional with the number of columns set to 1. So, Mat::dims is always &gt;= 2 (can also be 0 when the array is empty). 其实这就是在上面提到的。 从函数返回值获取OpenCV 中有很多函数，它们的返回值是 Mat，我们可以通过复制构造函数或赋值操作符来得到这样的对象，如我们常用的 imread 函数。还有许多常用的函数，现在就举一些例子。 create这是 Mat 的一个重要的成员函数，它有以下四种签名：1234void create (int rows, int cols, int type)void create (Size size, int type)void create (int ndims, const int *sizes, int type)void create (const std::vector&lt; int &gt; &amp;sizes, int type) 跟构造函数很类似，就是不能指定初始值。为什么说它重要呢？因为有了它，就可以我们就可以很方便、轻易的写一些函数，比如官方的 这个例子 ：1234Mat color;...Mat gray;cvtColor(color, gray, COLOR_BGR2GRAY); 你不再需要去关心为输出的对象构造好行与列，因为这些函数都会调用 create 这个函数。就像它所说的一样，调用 create 时会经过以下的步骤： If the current array shape and the type match the new ones, return immediately. Otherwise, de-reference the previous data by calling Mat::release. Initialize the new header. Allocate the new data of total()*elemSize() bytes. Allocate the new, associated with the data, reference counter and set it to 1. 当创建的新对象的尺寸与原来的不同时，才会重新分配新的空间。 operator()Mat 类重载了 operator() 操作符，以便于获取图像的一部分（感兴趣区域）。它具有以下四种签名：1234Mat operator() (Range rowRange, Range colRange) constMat operator() (const Rect &amp;roi) constMat operator() (const Range *ranges) constMat operator() (const std::vector&lt; Range &gt; &amp;ranges) const 如通过以下代码来获取一个图像的感兴趣区域：12345678Mat A = imread("opencv.jpg", 1);// 以下两种写法等效Mat B = A(Range(0,100),Range(20,200));// Mat B = A(Rect(20, 0, 180, 100));imshow("A", A);imshow("C", B); 运行效果如下： 复制 Mat因为 Mat 使用了引用计数，当你使用复制构造或赋值操作符的时候，它们实际上只是把矩阵数据的指针指向了相同的地址，共用同一片内存，所以对任意一个 Mat 对象做修改，也会影响到其它指向相同矩阵数据区域的对象（如果有）。我们运行一下以下这段代码：123456789101112131415161718192021222324#include &lt;opencv2/opencv.hpp&gt;using namespace cv;int main()&#123; Mat A = imread("opencv.jpg", 1), C; // 复制构造 与 赋值操作符 Mat B(A); C = A; // 显示三个图像 imshow("A1", A); imshow("B1", B); imshow("C1", C); // 对 A 进行归一化模糊后，再显示三个图像 blur(A, A, Size(9, 9)); imshow("A2", A); imshow("B2", B); imshow("C2", C); waitKey();&#125; 运行结果如下：我们可以看到，我们对 A 进行了模糊操作，而 B 与 C 也同时“变得”模糊了！这里加了个引号，是因为它们仨本身就是共用了同一个矩阵数据呀！当然是相同的啦！注意：即使是用一个矩阵的一部分去构造另一个矩阵，它们的矩阵数据的指针指向的也是同一片区域。也就是说，对原图像的改变，或对部分图像的改变，都会使矩阵本身的数据改变。例如我们对部分进行模糊处理：1234567891011Mat A = imread("opencv.jpg", 1);Mat B = A(Range(0,100),Range(20,200));imshow("A", A);imshow("B", B);// 对部分图像进行模糊处理blur(B, B, Size(9, 9));imshow("A2", A);imshow("B2", B); 运行效果如下：看到了吧？原图的那个部分最后也变模糊了。那么如果我们真的想复制一份 Mat，让他们各自有各自的矩阵数据怎么办呢？很简单，Mat 为我们提供了两个成员函数：clone 和 copyTo ： clone 1Mat clone () const copyTo 12void copyTo (OutputArray m) constvoid copyTo (OutputArray m, InputArray mask) const 他们都会将整个 Mat 的信息包括矩阵数据复制一份，所以对复制出来的新对象进行操作，就不会影响到原来的矩阵数据了。值得一提的是 copyTo 的第二个版本，多了一个 mask 参数，这个有什么用呢？当然有用啦，这就是为了方便我们处理那些感兴趣区域（ROI）呀！官方文档 给的说明是： Operation mask. Its non-zero elements indicate which matrix elements need to be copied. The mask has to be of type CV_8U and can have 1 or multiple channels. 按照我目前的理解，就是把你需要的颜色抠出来。比如我们把 OpenCV 图标上面那个红色的圈圈抠出来，我们就用一张灰度图，背景是黑色（0），要抠的地方是白色（255），这样要抠的地方就会原封不动的复制出来了。部分代码如下：123456789101112Mat src = imread("opencv.jpg", 1);Mat out;Rect r1(Rect(50, 0, 120, 100));Mat mask = Mat(src.rows, src.cols, CV_8UC1, Scalar(0));mask(r1).setTo(255);src.copyTo(out, mask);imshow("src", src);imshow("mask", mask);imshow("out", out); 运行效果如下：我们还可以改一下 mask 的参数，看看会有什么效果，比如我们用一个三通道的把背景改为蓝色的 mask 图：123Rect r1(Rect(50, 0, 120, 100));Mat mask = Mat(src.rows, src.cols, CV_8UC3, Scalar(255,0,0));mask(r1).setTo(255); 运行效果如下：可能会疑惑：为什么绿色那个圈圈经过这个 mask 后变成黑色了呢？我目前的理解是：从绿色里面（0,255,0）提取蓝色（255,0,0），在蓝色的分量上没有值，所以提取出来的就是黑色了（0,0,0）。 元素值的读写前面我们讲的都是整个 Mat 的创建或复制，那如果我们想对 Mat 里的矩阵的元素值进行访问或修改，该怎么做呢？ 矩阵的输出首先，我们简单的了解一下矩阵的输出，这有助于我们直观的感受矩阵中的数据。 Mat 重载了 operator&lt;&lt; 输出操作符，所以我们可以很方便的就得到格式化输出的结果：123// 声明一个 3x2 的三通道的矩阵Mat mat(3, 2, CV_8UC3, Scalar(0, 128, 255));std::cout &lt;&lt; "mat = " &lt;&lt; mat &lt;&lt; "\n"; 输出如下：123mat = [ 0, 128, 255, 0, 128, 255; 0, 128, 255, 0, 128, 255; 0, 128, 255, 0, 128, 255] OpenCV 还提供了其它很多格式化输出的形式，如 Python、CSV、numpy 等，这里不作为重点，如有兴趣可以看 这里 的例子，自己尝试。 使用 at() 函数Mat 提供了许多的 at 的重载版本对元素值进行读写，就不一一列举了，可以在 这里 查看。需要注意的是，at 需要我们指定一个 _Tp 模板类型，即如文档所说，你需要这样使用： If matrix is of type CV_8U then use Mat.at&lt;uchar&gt;(y,x). If matrix is of type CV_8S then use Mat.at&lt;schar&gt;(y,x). If matrix is of type CV_16U then use Mat.at&lt;ushort&gt;(y,x). If matrix is of type CV_16S then use Mat.at&lt;short&gt;(y,x). If matrix is of type CV_32S then use Mat.at&lt;int&gt;(y,x). If matrix is of type CV_32F then use Mat.at&lt;float&gt;(y,x). If matrix is of type CV_64F then use Mat.at&lt;double&gt;(y,x). 我们可以这样来创建一张黑白过渡的图片：123456789Mat img(255, 255, CV_8UC1);for (int r = 0; r &lt; img.rows; ++r)&#123; for (int c = 0; c &lt; img.cols; ++c) &#123; img.at&lt;uchar&gt;(r, c) = r % 255; &#125;&#125;imshow("black2white", img); 运行效果如下： 这是由上至下过渡的，我们还可以弄成从左到右的，只需要把 r % 255 改成 c % 255 就可以啦！当然，at 的模板参数可不是只能用上面所说的那些，我们要保证的是，数据类型能够一一对应。比如我们在弄一个三通道的图像：12345678910111213Mat img(510, 510, CV_8UC3);for (int i = 0; i &lt; img.rows; ++i)&#123; for (int j = 0; j &lt; img.cols; ++j) &#123; Vec3b pixel; pixel[0] = (i + j) % 255; pixel[1] = j % 255; pixel[2] = i % 255; img.at&lt;Vec3b&gt;(i, j) = pixel; &#125;&#125;imshow("what", img); 运行效果如下：你还可以继续更多有趣的尝试！ 使用迭代器如果你熟悉 STL，那么你也一定很喜欢迭代器的读写方式——安全，易写，不易出错。Mat 类也向我们提供了它的迭代器类型 MatIterator_，接下来我们就使用迭代器的方法来生成一张随机色彩的图像吧！1234567891011Mat img(400, 400, CV_8UC3);MatIterator_&lt;Vec3b&gt; iter;for (iter = img.begin&lt;Vec3b&gt;(); iter != img.end&lt;Vec3b&gt;(); ++iter)&#123; Vec3b pixel; pixel[0] = rand() % 255; pixel[1] = rand() % 255; pixel[2] = rand() % 255; *iter = pixel;&#125;imshow("what", img); 运行效果如下:有点像电视雪花啊哈哈，每次运行的结果肯定都一样的，不过都是乱七八糟的了。 使用指针Mat 同样提供了一个 ptr 函数及其的许多重载版本，可以在 这里 查看。使用指针确实是更容易出错，因为 C-style 指针是不带范围检查的。不过我觉得这点小事也怕干脆就别学了。听说用指针的效率会更高，当然我现在还没有进行充足的测试，有兴趣的朋友可以自己测试一下。我就举跟使用 at 一样的例子吧：12345678910Mat img(255, 255, CV_8UC1);for (int r = 0; r &lt; img.rows; ++r)&#123; uchar* p = img.ptr&lt;uchar&gt;(r); for (int c = 0; c &lt; img.cols; ++c) &#123; p[c] = r % 255; &#125;&#125;imshow("black2white", img); 上述代码同样是实现了一个黑白过渡的图像生成。 Mat_ 类有没有觉得上面很多 at&lt;uchar&gt;， ptr&lt;uchar&gt; 的写法很烦？而且更重要的是，这是不能做编译器检查的，也就算说，你写成以下代码：123456789Mat img(255, 255, CV_8UC1);for (int r = 0; r &lt; img.rows; ++r)&#123; for (int c = 0; c &lt; img.cols; ++c) &#123; // 这里应为 uchar img.at&lt;double&gt;(r, c) = r % 255; &#125;&#125; 编译期是没法检查这个类型错误的，直到运行期才会炸掉。Mat_ 类就是来解决这个问题的，Mat_ 类就是对 Mat 的一个封装，加上了一个模板参数用于指定类型，这样就可以免去很多麻烦和造成错误的可能。例如以下一个例子：1234567891011Mat img(255, 255, CV_8UC1);Mat_&lt;uchar&gt; img2(img); // Mat_ 对象for (int r = 0; r &lt; img2.rows; ++r)&#123; auto p = img2.ptr(); // 不需要指定类型了 for (int c = 0; c &lt; img2.cols; ++c) &#123; img2(r, c) = r % 255; // 可以使用 Matlab 风格 &#125;&#125;imshow("black2white", img2); 小结Mat 是整个 OpenCV 中非常重要的一个数据结构，它还有许多操作，以上只提到了冰山一角，可以从 这里 获取更多的信息。不断的练习与思考，就是进步的唯一途径。]]></content>
      <categories>
        <category>opencv</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>opencv</tag>
        <tag>visual studio</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[OpenCV 笔记（一）：图像视频处理入门]]></title>
    <url>%2F2017%2F07%2F06%2Fp1707062%2F</url>
    <content type="text"><![CDATA[第一个程序在 上一篇 中，我们运行了一个程序，显示一张图片：12345678#include &lt;opencv2/opencv.hpp&gt;using namespace cv;int main() &#123; // 把下面的 1.jpg 改成自己的图片的名称 Mat img = imread("1.jpg"); imshow("图片", img); waitKey();&#125; 其实不要看短短几行就实现了这个功能，但背后的工作其实是非常复杂繁多的。我们一点点来解释。 首先第一行，是一个 #include 声明：1#include &lt;opencv2/opencv.hpp&gt; 其实 OpenCV 是有很多模块的，各个模块都有其对应的头文件，而 opencv.hpp 这个头文件就包含了各个模块的头文件，所以这是一个最简洁的写法。 然后可能会有人指出，我在 之前的一篇博客 中，不是说不要使用 using namespace 吗？为什么这里又出现了呢？这是因为我是为了写一个 demo，我就是最终的用户了啊！不违背我的原则 =v= 接下来，声明并初始化了一个 Mat 对象，这个 Mat 对象是 OpenCV 中的 n 维密集数值阵列，可用于储存矩阵、灰度或彩色图像等，我们暂时不深入去了解它，这个部分会放到后面。初始化调用了一个函数 imread，我们来看看这个函数的声明：1Mat imread(const String &amp;filename, int flags = IMREAD_COLOR) 这个函数从一个文件中加载图片，第一个参数是文件名，第二个参数是打开的模式，它有一个缺省值 IMREAD_COLOR，代表将图像转换为三通道的彩色图像，一般使用默认值即可，想了解更多模式可以看 这里。 然后是一个 imshow 函数，这个函数会创建一个窗口并显示图像，我们看看它的函数声明：1void imshow(const String&amp; winname, InputArray mat); 第一个参数代表窗口的标题，第二个参数代表储存图像的对象。 最后是一个 waitKey 函数，它的函数声明是这样的：1int waitKey(int delay = 0) 它用于等待用户输入一个键并返回，delay 代表等待的毫秒数，缺省值为 0，当 delay 为非正数（&lt;=0）时，代表的就是无限等待直到用户输入一个键。 以上就是这部分代码的介绍，其中，imread 属于图像读写部分，可以在 这里 查看更多相关函数的信息；imshow 和 waitKey 属于 GUI 部分，可以在 这里 查看更多相关函数的信息。 图像处理我们有了一些直观的认识之后，在来一些感观的认识。官网给出了非常多的关于图像处理的 教程，我们可以都尝试一下，并且它每个例子下面都会有 Explanation，可谓是非常良心了。如果看英文不顺利的童鞋，可以看对应的中文网的教程，会有一些出入，但大体上差不多。 视频处理我们也可以用一个简短的程序，就能实现一个视频的读取：1234567891011121314151617181920#include &lt;opencv2/opencv.hpp&gt;using namespace cv;int main()&#123; // 将 1.mp4 替换成自己的视频文件 VideoCapture capture("1.mp4"); // 循环显示每一帧 while (1) &#123; Mat frame; // 读取当前帧到 frame capture &gt;&gt; frame; if (frame.empty()) break; imshow("视频", frame); // 每一帧之间的间隔(ms) waitKey(20); &#125; waitKey();&#125; 运行后它就能像一个播放器一样“播放”整个视频啦！试试看把1VideoCapture capture("1.mp4"); 这一句改成1VideoCapture capture(0); 会发生什么吧 ^_^更多视频处理相关内容可以看 这里 哦！ 结语OpenCV 毕竟是一个库，我们学习它，没必要像教科书式的，按顺序去学习它的各个模块、功能。我们想做什么，就直接去做，要用到什么，我们就查文档、查资料。在实践中学习，也是快速成长的一种方式。]]></content>
      <categories>
        <category>opencv</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>opencv</tag>
        <tag>visual studio</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[OpenCV 笔记（零）：启程]]></title>
    <url>%2F2017%2F07%2F06%2Fp1707061%2F</url>
    <content type="text"><![CDATA[OpenCV 是一个计算机视觉库，它提供了很多图像处理、机器学习等通用的算法，而且提供了 C++, Python, Ruby 等语言的接口。我们可以在 官网 上查看你想知道的信息。现在最新版本为 3.2.0，用过之后才感觉 OpenCV 的强大啊。 环境搭建很多人想用 OpenCV，但或许就栽到了环境搭建这第一步了，但我觉得现在这个版本搭建起来还是挺方便的，在官网上有各种系统和编译器的搭建指导（点这里）。我使用的是 Windows + VS2017，所以在接下来的系列文章中，没有特殊说明，我使用的都是 C++ 接口。环境搭建主要参考了 这篇教程 和 这篇教程。 首先我们可以在 RELEASES 页面下载最新的版本，我下载的是 Win pack。下载完成后，我们打开它解压到一个目录: 比如我解压到 H:\ ,那么在 H:\ 目录下就会有个 opencv 文件夹，我们打开它可以看到里面有一个 build 文件夹，里面是已经构建好的各种东西。build 文件夹里面的 include\opencv2 就是 OpenCV 的各种头文件。还有个 x64\vc14，这是给 vs2015（或以上）在 x64 平台上使用的各种东西（.dll,.lib文件等），我们会注意到，有一些文件非常相似，就是结尾有没有 d 的差别，如 opencv_world320.lib 和 opencv_world320d.lib，要注意，以 d 结尾的，是给 Debug 模式使用的，没有 d 结尾的则是给 Release 模式使用的，要注意这个区别，下面会用到。 接下来就是配置环境变量，可以参考 这个部分 和 这个部分，当然如果你不想看，我可以简单的告诉你要做的事情： 先添加一个环境变量指定到 OpenCV 的目录这里不一定要按上面教程上的来，例如我是添加了一个 OpencvDir 变量，值为 H:\opencv\build，为什么要加这个环境变量呢？其实也可以用绝对路径，添加这个环境变量的目的是当你 opencv 这个目录路径变化时，不用全部与之相关的东西都要改，只需要改这个环境变量的值就可以了。 把 opencv\build\bin 路径加入到系统的 Path 中这是因为要使用到里面的动态库（.dll文件） 配置 vs 项目属性打开 vs，新建一个项目，我们先把解决方案配置为 Debug|x64。要使用 opencv，简单来说就是要解决两件事： 告诉编译器在哪里找到 opencv 库 告诉链接器在哪里找到 opencv 库里面各种函数与数据结构的原型具体要做的有三件事：打开项目属性，在 C/C++ -&gt; 常规(General) 选项卡中，编辑 附加包含目录(Additional Include Directories)，加入 opencv\build\include 这个目录，对应我自己的环境变量，我就加入 $(OpencvDir)\include 。打开 链接器(Linker) -&gt; 常规(General) 选项卡，编辑 附加库目录(Additional Library Directories），加入 opencv\build\x64\vc14\lib 这个目录，对应我自己的环境变量，我加入的是 $(OpencvDir)\x64\vc14\lib 。打开 链接器(Linker) -&gt; 输入(Input) 选项卡，编辑 附加依赖项(Additional Dependencies)，然后把 x64\vc14\lib 里面的 opencv_world320d.lib 这个文件加入进去，因为我们配置的是 Debug 模式，就加入对应的这个 .lib 文件。 至此，OpenCV 在 vs 上的环境算是搭建好啦！有一些需要注意的点：※ 如果在打开 vs 的时候去修改了环境变量，vs 不会自动更新，需要重启 vs。 使用 OpenCV我们马上来运行一个简单的例子吧：显示图片。首先，找一张图片放到跟 main() 函数同级的目录下，然后输入以下这段代码：12345678#include &lt;opencv2/opencv.hpp&gt;using namespace cv;int main() &#123; // 把下面的 1.jpg 改成自己的图片的名称 Mat img = imread("1.jpg"); imshow("图片", img); waitKey();&#125; 按下 Ctrl + F5 运行，成功后你就能看到这样的画面啦！ 学习 OpenCV刚搭建好环境，我就把 Tutorials 里的 Image Processing 部分基本都跑了遍，包括平滑处理、形态变换、二值化、加边框等等，还是挺好玩，也挺好奇的。 耐心探索，OpenCV 还有非常多强大的功能，我主要会通过以下三个网站学习： 官方网站：http://opencv.org/ OpenCV 中文网：http://www.opencv.org.cn/ CSDN OpenCV 知识库：http://lib.csdn.net/base/opencv 可以开始尽情的玩耍啦 o(￣▽￣)ブ]]></content>
      <categories>
        <category>opencv</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>opencv</tag>
        <tag>visual studio</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[C++之那些年踩过的坑(四)]]></title>
    <url>%2F2017%2F06%2F10%2Fp1706101%2F</url>
    <content type="text"><![CDATA[事情起因昨天花了一些时间给我的 MyTinySTL 重新弄了一套构建方案，主要是写了一个 CMakeLists.txt（原来是写了一个简单的 Makefile）。现在呢，在 linux/osx 下，就通过 cmake 来构建生成对应的 Makefile，然后再 make；在 Windows 下，就直接使用 vs 的 .sln 配置文件。具体可以看项目说明。 问题浮现然后修改完持续集成的脚本配置之后，却出现了一个奇怪的问题：在 linux 下的构建全部失败了！打开查看里面的信息，基本都是这样的错误提示：gcc提示是这样的：clang提示是这样的：都是在 &lt;cstring&gt; 这个文件出错了，可这是标准库文件，怎么会出错呢？而且错误信息也很奇怪。我又去找代码查找有没有什么可能出错的地方，很久也没找到。然后跟别人请教了一下，某大神直击要害地问我：“你是不是有一个 string.h 文件？” 找到缘由c11 中有这样一段话：注意框出来的地方： If a ﬁle with the same name as one of the above &lt; and &gt; delimited sequences, not provided as part of the implementation, is placed in any of the standard places that are searched for included source ﬁles, the behavior is undeﬁned. 也就是说，如果有一个文件跟上述标准库文件重名，然后被包含在源文件中时，是未定义行为。恰好，因为我写的是 tinystl，所以我写了一个 string.h，没错，就是跟我框出来那个 &lt;string.h&gt; 重名了！！解决办法就是 —— 换个名字~ 是的，然后我就把 string.h 改成了 astring.h，问题就解决了！( ╯□╰ )一波三折，谨以记之，提醒自己做过的傻x事情。]]></content>
      <categories>
        <category>C++之那些年踩过的坑</category>
      </categories>
      <tags>
        <tag>C++</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[8086汇编：组织程序]]></title>
    <url>%2F2017%2F05%2F31%2Fp1705312%2F</url>
    <content type="text"><![CDATA[前面我们介绍了8086汇编中一些常用的、基本的、重要的概念，现在我们就要把这些东西组装起来。 定义变量1var_name db/dw/dd expression 其中的 var_name 就是一个变量，db/dw/dd是伪指令助记符，用于指定字节/字/双字，表达式可以为一个常量，一个字符串等。12345data segment num dw 0 msg db 'please input a num' len db $-msg1data ends 操作数&#39;?&#39;用于保留储存空间，但不存入数据。12adc db 0, 1, ?, ?, ?len db ? 操作数字段还可以利用复制操作符 dup 来复制重复元素。12abc db 0, 1, 3 dup (?)ssr db 5 dup (0, 1, 2) 程序的开始与结束1. end 伪指令1end label end 伪指令标识着整个程序的结束，end 后面的操作数指定程序执行的起始地址。所以，一般主程序模板必须 end 后面指定操作数，然后在操作数放到代码段的开头，如：1234567891011assume cs:code, ds:datadata segments ; ... data endscode segmentsstart: ; 程序执行的起始地址 ; ...code ends end start ; 表明 start 为程序执行的起始地址 2. assume 伪指令8086存储器是分段的，分了段之后，就必须指明段和段寄存器之间的关系，这由假设语句 assume 实现。1assume segment_reg : segment_name [,...] 其中，segment_reg 必须是 cs, ds, ss, es 中的一个，segment_name 则是定义的段名。 注意：assume 伪指令只是指定了某个段分配给某个段寄存器，但它并没有将段地址装入段寄存器，所以在代码段中，还要将段地址装入段寄存器。 3. 定义段123segment_name segment [align_type] [combine_type] [use_type] ['class']...segment_name ends 其中，[]代表是可选的，可以缺省的，但各项顺序不能错，一般不需要我们指定。 4. 定义过程12345process_name proc [near/far]...process_name endp 在 proc 后面可以指定 near（默认）—— 在段内被调用，far—— 可以被段外调用。 在 masm 中，过程含义跟子程序一样，可以通过 call 来调用，过程返回自动调用 ret。 子程序子程序跟过程段类似，通过 call 调用，ret 来返回，重点要注意现场保护，参数传递等问题。123456789101112name: ; 要用到的寄存器压栈 push ax push bx ; ... ; 子程序过程... ; 恢复寄存器 pop bx pop ax ret 简化段简化段首先要定义程序的存储模型： 存储模型 说明 适用系统 tiny（微型） 所有数据和代码都放在一个段内，其访问都为NEAR型，整个程序≤64K，并会产生.COM文件 MS-DOS small（小型） 所有代码在一个64KB的段内，所有数据在另一个64KB的段内（包括数据段,堆栈段和附加段） MS-DOSWindows medium（中型） 所有代码＞64K时可放在多个代码段中，转移或调用可为FAR型。所有数据限在一个段内，DS可保持不变 MS-DOSWindows compact（紧凑型） 所有代码限在一个段内，转移或调用可为NEAR型。数据＞64K时，可放在多个段中 MS-DOSWindows large（大型） 允许代码段和数据段都可超过64K，被放置在有多个段内，所以数据和代码都是远访问 MS-DOSWindows huge（巨型） 单个数据项可以超过64K，其它同large模型 MS-DOSWindows 说明：small 模型是一般应用程序最常用的一种模型，因为只有一个代码段和一个数据段，所以数据和代码都是近访问的。所以一般将 .model 指定为 small 就可以了。 简化段伪指令： 伪指令 功能 说明 .code [段名] 创建一个代码段 隐含段名为 @code .data 创建一个数据段 隐含段名为 @data .stack [大小] 创建一个堆栈段并指定大小 隐含段名为 @stack，并形成ss和sp的初值 例：12345678910111213.model small.stack 100h.data ; 定义数据段 ;....code ; 定义代码段main: mov ax, @data ; 数据段地址 mov ds, ax ; ... mov ax, 4c00h int 21hend main 其他伪操作1. 基数控制汇编程序默认的数为十进制数，还可以由以下方式指定基数： 二进制： 01001000b 八进制： 123q 或 123o 十进制： 1234d 十六进制： 1234h / 0ffffh （第一个为字母时前面加0） 2. &#39;$&#39; 地址计数器&#39;$&#39;保存了当前汇编指令的偏移地址。 常用用途： 计算字符串长度 123456data segment str1 db 'hello' len1 db $-str1 ; 5 str2 db 'world hahaha' len2 db $-str2 ; 12data ends 跳转 1jne $+6 ; 跳转到 jne 指令所在地址 + 6]]></content>
      <categories>
        <category>汇编</category>
      </categories>
      <tags>
        <tag>汇编</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[8086汇编：指令系统（下）]]></title>
    <url>%2F2017%2F05%2F31%2Fp1705311%2F</url>
    <content type="text"><![CDATA[控制转移指令1. jmp（无条件转移）123jmp label ; 跳到 label 的地址jmp reg16/mem16 ; 段内转移，ip = reg16/[mem16]jmp mem32 ; 段间转移，cs = mem32高字，ip = mem32低字 可以加上 short, near, far 等标识符，分别表明是短转移（ip修改范围 -128 ~ 127）、近转移（ip修改范围 -32768 ~ 32767）、远转移（cs:ip都修改）。 2. jcc（条件转移）1jcc label ; 若成立，ip 修改为 label 的偏移地址（-128~127），否则忽略该指令 简单测试条件的jcc指令 jcc 指令 描述 转移条件 jz/je 为零（相等）时转移 zf = 1 jnz/jne 非零（不等）时转移 zf = 0 jc/jb 有进位时转移 cf = 1 jnc/jnb 无进位时转移 cf = 0 js 为负时转移 sf = 1 jns 为正时转移 sf = 0 jo 溢出时转移 of = 1 jno 不溢出时转移 of = 0 jp/jpe 奇偶位为1时转移 pf = 1 jnp/jpo 奇偶位为0时转移 pf = 0 jcxz cx为0时转移 cf = 0 带符号数比较的jcc指令 jcc 指令 描述 转移条件 jg/jnle 大于（不小于等于）时转移 sf = of 且 zf = 0 jge/jnl 大于等于（不小于）时转移 sf = pf jl/jnge 小于（不大于等于）时转移 sf ≠ of jle/jng 小于等于（不大于）时转移 sf ≠ of 且 zf ≠ 0 无符号数比较的jcc指令 jcc 指令 描述 转移条件 ja/jnbe 高于（不低于等于）时转移 cf = 0 且 zf = 0 jae/jnb 高于等于（不低于）时转移 cf = 0 jb/jnae 低于（不高于等于）时转移 cf = 1 jbe/jna 低于等于（不高于）时转移 cf = 1 或 zf = 1 3. loop（循环）123loop label ; cx=cx-1, 若cx!=0则转到labelloopz/loope label ; cx=cx-1，若cx!=0且zf=1则转到labelloopnz/loopne label ; cx=cx-1，若cx!=0且zf=0则转到label label的相对偏移量必须在-128~127之间 4. call（调用）/ ret（返回）123call label ; 段内直接调用，ip入栈call reg16/mem16 ; 段内间接调用，ip入栈call mem32 ; 段间间接调用，cs和ip入栈 CPU执行call指令时，分两步操作： 将ip（16位操作）或cs:ip（32位操作）入栈 转移 12ret ; 近转移retf ; 远转移 CPU执行ret指令时，相当于1pop ip CPU执行retf指令时，相当于12pop ippop cs call指令与ret指令必须配合使用。 标志处理指令 指令 功能 clc cf = 0 stc cf = 1 cmc cf = not cf cld df = 0 std df = 1 cli if = 0 sti if = 1 串处理指令串操作指令有以下特点： 用 si 寄存器寻址源操作数，用 di 寄存器寻址目的操作数 源操作数隐含的段寄存器为 ds，但允许段超越；目的操作数的段寄存器约定为 es，不允许段超越 每当串操作指令执行后，自动执行源变址或目的变址，当 df=0 时，地址增量为正，否则为负。增量的大小与操作数类型有关，字节为1，字为2，双字为4 ※ 这也表明了为什么si是源变址寄存器，di是目的变址寄存器，而ds为数据段，es为附加段。 1. rep（重复串操作直到cx为0）rep常与 [movs,stos,lods,ins,outs] 指令配合使用。1rep string op[movs/stos/lods/ins/outs] 执行过程如下： 若cx=0，退出rep，否则往下执行 dec cx 执行 string op 返回 1 2. movs（串传送）movs后加上b,w等表示操作类型为字节，字。12movsb dst, src ; 传送字节movsw dst, src ; 传送字 作用： es:[di] = ds:si，将ds:si所指的串复制cx个字节/字到es:di所指的内存单元中，当df=0时，每次操作后si和di递增size，否则递减size。其中，size等于1（字节）或2（字）。 例：12345678910111213141516171819202122232425262728293031assume cs:code, ds:data, es:extradata segment srcstr db 'hello world!$'data endsextra segment dststr db 13 dup(0)extra endscode segmentmain: mov ax, data mov ds, ax mov ax, extra mov es, ax lea si, srcstr lea di, dststr mov cx, 13 cld rep movsb lea dx, dststr mov ah, 9 int 21h mov ax, 4c00h int 21hcode endsend main 3. lods（串载入）/ stos（串存储）12lodsb ; 载入字节lodsw ; 载入字 作用：将 ds:si 所指串的值复制到 al/ax 中，然后根据df增加或减少si。 12stosb ; 存入字节stosw ; 存入字 作用：将 al/ax 的值 复制到 es:di 所指的内存单元中，然后根据df增加或减少di。 例：123456789101112131415161718192021assume cs:code, es:extraextra segment buf db 10 dup(0)extra endscode segmentmain: mov ax, extra mov es, ax mov ax, 20h lea di, buf mov cx, 10 cld rep stosb mov ax, 4c00h int 21hcode endsend main 4. cmps（串比较）12cmpsb ; 比较源串与目的串的一个字节cmpsw ; 比较源串与目的串的一个字 作用：将 ds:si 所指串的值与 es:di 所指串的值进行比较，并根据比较结果（两个值相减）设置标志位，然后，对 si 和 di 做相应的调整。]]></content>
      <categories>
        <category>汇编</category>
      </categories>
      <tags>
        <tag>汇编</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[8086汇编：指令系统（上）]]></title>
    <url>%2F2017%2F05%2F30%2Fp1705301%2F</url>
    <content type="text"><![CDATA[8086汇编常用的指令，从功能上分，一般分为： 数据传送指令 算术指令 逻辑指令 控制转移指令 标志处理指令 串处理指令 这些指令书上网上都有很多介绍，我就不会全部详细介绍，只挑一些常用的、需要注意的重点说明。 数据传送指令1. mov（传送）1mov dst, src 有以下需要注意的地方： dst 和 src 类型必须匹配，无法确定类型时要显式指定类型。 123456mov ax, 1 ; 正确！立即数会自动扩展类型mov ax, bl ; 错误！类型不匹配mov ax, [0] ; 可以运行，但不是预期结果，原因见 2mov ax, ds:[0] ; 正确！ax是字类型，ds:[0]也获取字类型mov ds:[0], 1 ; 错误！无法确定类型mov byte ptr ds:[0], 1 ; 正确！指定了访问的内存单元是一个字节单元 形如 mov ax, [0] 这类指令 Debug加载运行 和 masm编译器对它们的解释不同，Debug 将[idata]解释为一个内存单元，idata是内存单元的偏移地址；而编译器将[idata]解释为idata，一个立即数。所以，在汇编源程序中，要访问一个内存单元，并且 [...] 里面是一个立即数，必须要用以下两种方法： 将偏移地址放进 bx 寄存器（或si,di）中，然后用 [bx] 来访问内存单元。（用 bx, si, di 时，默认段寄存器在 ds 中， 用 bp 时，默认段寄存器在 ss 中） 12mov bx, 1mov ax, [bx] 在 [] 前面显示指定段寄存器。 1mov ax, ds:[0] dst 和 src 不能同时为内存单元和段寄存器。 不能将立即数传给段寄存器，段名也是立即数，但可以将内存单元数据传给段寄存器。 1234567891011121314151617assume cs:code, ds:datadata segment address dw 1234hdata endscode segmentmain: mov ds, 1234h ; 错误！不能将立即数传给段寄存器 mov ds, data ; 错误！段名也是一个立即数 mov bx, 0 mov ds, [bx] ; 可以 mov ds, address ; 可以，address会被编译成一个地址，同上一条指令类似 mov ax, 4c00h int 21hcode endsend main 目的操作数 dst 不能为立即数或 cs(代码段寄存器)。 mov 指令不影响标志位。 2. push（入栈） / pop（出栈）12push reg16/segreg/mem16pop reg16/segreg/mem16 push 和 pop 的操作数可以是16位寄存器、段寄存器或者字内存单元。注意：不能 pop 到 cs(代码段寄存器)中。 以下操作都是合法的：123456push axpush dspush word ptr ds:[0]pop word ptr ds:[0]pop dspop ax 3. xchg（交换）1xchg object1, object2 交换8/16位寄存器/内存单元的值。 注意： 类型必须匹配。 交换数必须是通用寄存器（AX,BX,CX,DX,SI,DI）或内存单元。 两个交换数不能同时为内存单元。12345678xchg al, dl ; 可以xchg ax, bx ; 可以xchg ax, word ptr ds:[0] ; 可以xchg [bx], ax ; 可以xchg al, bx ; 错误！类型不匹配xchg ax, ds ; 错误！交换数不能为段寄存器xchg ds, ax ; 错误！交换数不能为段寄存器xchg byte ptr ds:[0], byte ptr ds:[1] ; 错误！两个都是内存单元 4. lea（传入有效地址 Load Effective Address）1lea reg16 mem 注意： 目的操作数必须为16位通用寄存器，不能是段寄存器 传入寄存器的是有效地址，而不是内容 1234lea dx, ds:[0] ; 可以lea dx, msg ; 可以，类似于上一条指令lea dl, ds:[0] ; 错误！必须为16位通用寄存器lea ds, ds:[0] ; 错误！不能为段寄存器 1lea dx, ds:[0] dx中保存了ds:[0]的偏移地址，若想得到ds:[0]的值，要用：1mov dx, ds:[0] 以下两条指令功能相同：12lea dx, msgmov dx, offset msg 算术指令加法指令1. add（加法）1add reg/mem, reg/mem/idata 作用：dst = dst + src注意：1） 两个操作数类型要匹配。 2）操作数不能同为内存单元。 2. adc（带进位加法）1adc reg/mem, reg/mem/idata 作用：dst = dst + src + cf注意：同 add。 3. inc（自增一）1inc reg/mem 减法指令1. sub（减法）1sub reg/mem, reg/mem/idata 作用：dst = dst - src注意：同 add。 2. sbb（带借位减法）1sbb reg/mem, reg/mem/idata 作用：dst = dst - src - cf注意：同 add。 3. dec（自减一）1dec reg/mem 4. neg（求补）1neg reg/mem 作用：dst = 0 - dst （即求相反数） 5. cmp（比较）1cmp reg/mem, reg/mem/idata 作用：根据 dst - src 的结果去影响标志位。注意：同 add。 乘法指令1. mul（无符号乘法）1mul reg/mem 作用：1）当操作数为8位时，乘数默认保存在 al 中，结果保存在 ax 中，ax = al × src。2）当操作数为16位时，乘数默认保存在 ax 中，结果的高16位保存在 dx 中，低16位保存在 dx 中，dx : ax = ax × src。 2. imul（带符号乘法）同 mul。 除法指令1. div（无符号除法）1div reg/mem 作用：1）当操作数为8位时，被除数默认保存在 ax 中，结果商保存在 al 中，余数保存在 ah 中。2）当操作数为16位时，被除数默认高16位保存在 dx 中，低16位保存在 ax 中，结果商保存在 ax 中，余数保存在 dx 中。 2. idiv（带符号除法）同 div。注意：除法指令可能会发生溢出，产生0号中断。可参考 http://www.doc88.com/p-5836860600548.html 。 逻辑指令逻辑运行指令1. and（与）/ or（或）/ not（非）/ xor（异或）1234and reg/mem, reg/mem/idataor reg/mem, reg/mem/idatanot reg/memxor reg/mem, reg/mem/idata 注意：1） 两个操作数类型要匹配。 2）操作数不能同为内存单元。 2. test（测试）1test reg/mem, reg/mem/idata 作用：将两个操作数相与（and）后的结果去影响标志位，若结果为0，则zf=1，否则zf=0。注意：1） 两个操作数类型要匹配。 2）操作数不能同为内存单元。常用用法：检查某位是否为1，e.g.: 检验ax是否为负数 12test ax, 8000hjnz l ; 是负数则跳转 检验ax是否为奇数 12test ax, 1jnz l ; 是奇数则跳转 判断大小写 123test ax, 00100000bjz label1 ; 大写则跳转jmp lable2 ; 小写则跳转 移位指令shl（逻辑左移）/ shr（逻辑右移）/ sal（算术左移）/ sar（算术右移）1234shl reg/mem, 1/clshr reg/mem, 1/clsal reg/mem, 1/clsar reg/mem, 1/cl 作用：将操作数各个二进制位向左（右）移动1（cl）位，最高（低）位移出到 cf，最低（高）位补0（sf）。注意：1）若移位数为1，可以直接写1，否则必须将移位数放入cl中并调用。2）无论移动多少次，cf等于最后移出位的值。逻辑移位跟算术移位的区别：算术移位是将数据看成是有正有负的补码数的运算，逻辑移位是将数据看成是无符号数。左移时，算术移位和逻辑移位最右端都是用0补充，操作相同，所以指令等价。右移时，算术右移最右端补sf（符号标志位），逻辑右移补0，所以指令不同。 循环移位指令rol（循环左移）/ ror（循环右移）/ rcl（带进位循环左移）/ rcr（带进位循环右移）1234rol reg/mem, 1/clror reg/mem, 1/clrcl reg/mem, 1/clrcr reg/mem, 1/cl 作用：循环移位时，将操作数各个二进制位向左（右）移动1（cl）位，最高（低）位同时移入cf和最低（高）位。带进位循环移位时，将操作数各个二进制位向左（右）移动1（cl）位，最高（低）位移入cf，原cf移入最低（高）位。注意：同移位指令。]]></content>
      <categories>
        <category>汇编</category>
      </categories>
      <tags>
        <tag>汇编</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[8086汇编：寄存器与寻址方式概述]]></title>
    <url>%2F2017%2F05%2F29%2Fp1705292%2F</url>
    <content type="text"><![CDATA[8086 计算机系统模型 通过系统总线，将计算机的各个部分链接在一起。CPU 则是这个体系的核心。 8086 CPU 内部模型： 寄存器8086 CPU 内部有14个可见的（程序员可操作）的寄存器，均为16位。分别为：AX, BX, CX, DX, SP, BP, SI, DI, IP, FLAG, CS, DS, SS, ES。这14个寄存器从用途上又分为三类：通用寄存器，控制寄存器和段寄存器。 通用寄存器4个数据寄存器：AX, BX, CX, DX AX (Accumulator)：累加寄存器 BX (Base)：基址寄存器 CX (Count)：计数寄存器 DX (Data)：数据寄存器 这4个寄存器又可分为8个独立的8位寄存器：AH, BH, CH, DH, AL, BL, CL, DL。 2个指针寄存器：SP, BP SP (Stack Pointer)：堆栈指针寄存器 BP (Base Pointer)：基址指针寄存器 2个变址寄存器：SI, DI SI (Source Index)：源变址寄存器 DI (Destination Index)：目的变址寄存器 段寄存器 CS (Code Segment)：代码段寄存器 DS (Data Segment)：数据段寄存器 SS (Stack Segment)：堆栈段寄存器 ES (Extra Segment)：附加段寄存器 控制寄存器 IP (Instruction Pointer)：指令指针寄存器 FLAG：标志寄存器 物理地址与有效地址物理地址(Physical Address)就是CPU查找内存单元时的一个地址，它是唯一的。内存单元物理地址可由段址和段内的偏移量确定，内存单元在段内的偏移量就叫做有效地址(Effective Address)。 8086 CPU内部有20根地址线，其编码区间为：0000H~FFFFFH，所以，它的寻址能力（可直接访问的物理空间）为1M字节（2^20 bits）。但是CPU内部为16位的结构，如果用16位寄存器来访问内存的话，表现出来的寻址能力却只有64K。为了能用16位寄存器来有效地访问1M的存储空间，CPU内部采用了两个16位的地址来合成一个20位的物理地址。最终，CPU就采用下面这种方式来合成一个物理地址： 物理地址 = 段地址 × 16 + 偏移地址 这样的方式用汇编表现出来就是： 段寄存器 : 偏移地址 比如，8086 CPU 要访问物理地址为 12000h(十六进制)的内存单元，就可以把 DS 设为 1000h，把 SI 设为 2000h，然后通过 DS:[SI] 的方式表达，地址加法器就会按照上面的公式算出物理地址 = 1000h × 16 + 2000h = 12000h。 四个段寄存器就组成以下四种表达： CS : IP 段寄存器CS指向存放程序的内存段，IP是用来存放下条待执行的指令在该段的偏移量，把它们合在一起可在该内存段内取到下次要执行的指令。 DS : ** 段寄存器DS指向数据段，在存取操作数时，由DS和一个偏移量合成就可得到存储单元的物理地址。该偏移量可以是具体数值、符号地址和指针寄存器的值等之一，具体情况将由指令的寻址方式来决定。 SS : SP 段寄存器SS指向用于堆栈的内存段，SP是用来指向该堆栈的栈顶，把它们合在一起可访问栈顶单元。另外，当偏移量用到了指针寄存器BP，则其缺省的段寄存器也是SS，并且用BP可访问整个堆栈，不仅仅是只访问栈顶。 ES : DI 段寄存器ES指向附加数据段，也用于数据的保存，一般与DS用法类似，在进行串操作时，其目的地址的段寄存器规定为ES。 寻址方式首先，要明确，什么是寻址方式。所谓寻址方式，就是讨论指令中操作数的来源问题。 根据操作数的不同来源来分类的话，寻址方式可以分为储存器方式和非储存器方式。下面着重讲 8086/8088 体系下的寻址方式。 8086 CPU 有七种基本的寻址方式： 立即寻址 寄存器寻址 直接寻址 寄存器间接寻址 寄存器相对寻址 基址变址寻址 相对基址变址寻址 其中，前两种方式属于非储存器寻址方式，后五种属于储存器寻址方式。除了这些基本寻址方式，还有一些其它的寻址方式，如固定寻址、I/O端口寻址等，这些不会重点介绍。 为了总结方便，我费尽精力做了一个表格： 寻址方式 指令特点 举个栗子 注意 立即寻址 用立即数给寄存器赋值 mov ax, 1234h 无 寄存器寻址 源操作数为寄存器 (1) mov num, ax(2) mov ax, bx 无 直接寻址 括号内一个立即数 (1) mov dx, [1000h](2) mov es:[1000h], dx 使用DS作为缺省段寄存器 寄存器间接寻址 括号内一个基址或变址寄存器(SI,DI,BX,BP) (1) mov dx, [di](2) mov [bp], cx 若有效地址在SI,DI和BX中，则以DS作为缺省段寄存器；若有效地址在BP中，则以SS作为缺省段寄存器 寄存器相对寻址 括号内一个基址或变址寄存器(SI,DI,BX,BP)+一个立即数 (1) mov ax, [bx+16](2) mov ax, 16[bx](3) mov es:[bx+5], al 若SI,DI和BX作为有效地址的一部分，则以DS作为缺省段寄存器；若BP作为有效地址的一部分，则以SS作为缺省段寄存器 基址变址寻址 括号内一个基址寄存器(BX,BP)+一个变址寄存器(SI,DI) (1) mov ax, [bx+si](2) mov ax, [si][bx](3) mov ds:[bp+si], al 若SI,DI和BX作为有效地址的一部分，则以DS作为缺省段寄存器；若BP作为有效地址的一部分，则以SS作为缺省段寄存器 相对基址变址寻址 括号内一个基址寄存器(BX,BP)+一个变址寄存器(SI,DI)+一个立即数 (1) mov ax, [bx+di+10h](2) mov ax, 10h[bx+di](3) mov ax, 10h[bx][di](4) mov ax, 10h[di][bx] 若SI,DI和BX作为有效地址的一部分，则以DS作为缺省段寄存器；若BP作为有效地址的一部分，则以SS作为缺省段寄存器]]></content>
      <categories>
        <category>汇编</category>
      </categories>
      <tags>
        <tag>汇编</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[8086汇编：前言]]></title>
    <url>%2F2017%2F05%2F29%2Fp1705291%2F</url>
    <content type="text"><![CDATA[为什么要学习汇编我觉得，对于一个合格的、资深的程序员来说，学习汇编和理解汇编是必不可少的。这里的汇编不特指某一平台的汇编，但其实核心内容都一样。 原因如下： 学习汇编帮助你理解高级语言的运行 深度调试时的需要 编译器不总是对的 尤其是 C/C++ 这类的编程语言，有很多坑，而一些书或者人只告诉你不能这样做，但是为什么不能这样做，只有汇编才能告诉你。 还有比如程序优化，只有通过反汇编，你才能确定，程序优化的结果。 以及一些编译器的坑，编译器也是人写的，不能100%正确，那你怎么确定是你的问题还是编译器的问题？只有汇编能救你。 学习汇编的好处@Skogkatt 在 这个回答 说： 学汇编不是说一定要用这玩艺做多牛鼻的事情, 问题的关键在于, 学透了汇编会使你真正理解计算机另外一方面, 如上面所说, 在工作中你迟早会在某个阴暗的角落遇到汇编. 不管你承认不承认, 现在的CPU没有直接跑高级语言的, 哪怕是虚拟机也都是类似汇编的指令集.当遇到崩溃分析, 性能优化甚至编译器抽风等等的时候, 汇编是你最后一根救命稻草.]]></content>
      <categories>
        <category>汇编</category>
      </categories>
      <tags>
        <tag>汇编</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[C++之那些年踩过的坑（附录一）]]></title>
    <url>%2F2017%2F05%2F28%2Fp1705282%2F</url>
    <content type="text"><![CDATA[注：本文来自于我的一篇博客 http://www.cnblogs.com/GodA/p/6639526.html ，修改而成。 这篇内容呀，其实是接上一篇的结尾提出的一个小问题而写的，如果你还没有看过那么你可以先看一下。 真相在上一篇的最后，我提到一个问题：代码优化。并留下了一个小小的测试：无符号数和有符号数的性能比较。不知道有没有童鞋去实验一下呢？那我们一起来做一个简单的实验：12345678910111213141516171819#include &lt;iostream&gt;#include &lt;chrono&gt;int main()&#123; /*unsigned */int a = 123456789; auto t1 = std::chrono::system_clock::now(); for (int i = 0; i &lt; 10000; ++i) &#123; a += 3; a -= 5; a *= 11; a /= 7; &#125; auto t2 = std::chrono::system_clock::now(); auto runtime = std::chrono::duration_cast&lt;std::chrono::nanoseconds&gt;(t2 - t1); std::cout &lt;&lt; "a = " &lt;&lt; a &lt;&lt; "\n"; std::cout &lt;&lt; runtime.count() &lt;&lt; "ns";&#125; 在上述代码在 VS 的 Debug 模式下运行，稳定后运行时间在 210000ns 左右，然后把注释去掉，再次运行，稳定后运行时间也是 210000ns 左右。在我在电脑上，计算有符号类型和无符号类型几乎是没有差别的，我相信在绝大多数的电脑上也是相同的结果。 类似的问题还有：浮点数的计算比整型数慢？对于这个问题，可以看一下这个问题：https://www.zhihu.com/question/26494062 。里面提供大量实验数据供参考。 无论如何，实际情况下就是，在当代，计算无符号数和有符号数的效率，甚至是计算浮点数和整数的效率，差异并不大。 我想说明什么呢？首先我要声明：我不是反对代码优化。而是对于有很多流传广泛的所谓的优化技巧，我觉得我们应该应该抱着学习探索的心态，而不是一味地追求一些没有什么意义的东西。有些优化，确实很精妙。但很多所谓的技巧，看起来的意思就是：做编译器的那群人都是傻逼。想优化我们的程序，这是正常的、应该的想法，但我们应该用科学的方法，而不是听了一些奇淫技巧，却不知道实际发生了什么。 其实很简单，探究性能瓶颈靠 profiling，探究代码背后的不为人知的故事看 assembly。我们先讲后面一个。 利用 assembly 探究代码怎么得到反汇编代码，我在 这一篇 当中有提到。比如我们研究有符号数和无符号数，先写一个程序：123456int main()&#123; /*unsigned */int a = 123456789; a = a / 13; std::cout &lt;&lt; a;&#125; 然后在 VS 下查看反汇编：12345678/*unsigned */int a = 123456789;00B4104E mov dword ptr [a],75BCD15h a = a / 13;00B41055 mov eax,dword ptr [a] 00B41058 cdq 00B41059 mov ecx,0Dh 00B4105E idiv eax,ecx 00B41060 mov dword ptr [a],eax 把注释去掉再看：12345678unsigned int a = 123456789;00C3104E mov dword ptr [a],75BCD15h a = a / 13;00C31055 mov eax,dword ptr [a] 00C31058 xor edx,edx 00C3105A mov ecx,0Dh 00C3105F div eax,ecx 00C31061 mov dword ptr [a],eax 几乎是一模一样的，最大的差别就是有符号数使用 idiv指令（带符号除法），无符号数使用 div指令（不带符号除法），而这两种指令，CPU 周期都是一样的。http://www.agner.org/optimize/instruction_tables.pdf 当然我不是说不用无符号数，而是说我们用什么要看场合，而不是你觉得用了性能更好，除非是被大众认可的或者你经过严谨的测试的。像对于某些书籍或者什么地方说，只要确定范围不为负数的，就用无符号类型，我是不认可的。如果你讲范围，那如果一个有符号类型不够用，那么通常它对应的无符号类型(相同的 bits)也不够用。比如你 std::int32_t 不够用，就应该用 std::int64_t，如果还不够，考虑写个 BigInteger类 吧。不过对于无符号和有符号类型，它们之间的性能在当代确实是几乎没有什么差别。那具体什么场合用什么呢？这个也不一定，比如一般来说： 对于位储存、位运算、模运算等，使用无符号类型 对于一般运算使用有符号类型 这一篇的目的，当然不只是谈有符号数和无符号数，而是想借题引出一些我的看法： 过早的优化是万恶之源 不要试图帮编译器优化 优化时不要去猜测，想当然得去优化自己“觉得”性能不好的地方 我们一点一点讲。 对于一个需求，我们应该先完成功能，若性能达不到要求之后，在确定瓶颈之后再去优化。过早优化，不仅让代码不直接，还容易出 bug，还可能对性能几乎没有影响。而且，我们优化时，应该关注大方向，确定大方向是正确的。比如写一个算法，我们首先应该确保 Big-O 的时间复杂度能达标，可以用 O(n) 的就不用 O(nlogn)，可以用 O(nlogn) 的就不用 O(n²)，而不是先在那里扣 i++ 还是 ++i。另外，不要想着去帮编译器优化，因为编译器是一堆比你强不知道多少的人写出来的，而对于一般人，想着去帮编译器优化，大部分是无效的，甚至是错的。比如有人学了一点点的 std::move，就老是想着 move move move 去提高性能，举个栗子，容易写出这样的代码：123456template &lt;typename T&gt;T create()&#123; auto object = new T(); return std::move(object);&#125; 确实运行不会错，但是，代码背后做的事情不一定就跟你想的一样，往往跟你想象的还不一样。有些情况编译器可以采用更好的办法，结果因为你那么一搞，迫不得已只能采用次一点的办法。可以看看 这个问题，就不赘述了。 还有比如说用异或来交换两个变量，有人会想，用位运算，不仅不需要创建临时变量，而且位运算一般不是更快嘛！对于这个问题，陈硕大大早有讲到，在 https://cloud.github.com/downloads/chenshuo/documents/CppPractice.pdf 的第 9 章。可以去看一下。 如果你已经看了上面的链接，那么你也就知道了，你（几乎）不会知道编译器做了什么，编译器可以做的优化超出你的想象（不过有的时候人能明显看出来的优化编译器却做不到，但影响不大），在我的 系列文章（二） 中也有强调了，不要试图帮助编译器去优化。若你想探究一小段代码背后不为人知的故事，就去看看反汇编！ 利用 profiling 探究瓶颈关于这个问题，我想贴上一个链接，是 蓝色大大 的一个回答：https://www.zhihu.com/question/56727144/answer/150555866。有非常详细清晰的介绍。 结语最后我希望我放的链接都有认真看呀！为什么我贴那么多链接呢？因为我说的话没有权威没人信啊！但大牛们说的总有参考价值了吧！ 暂时先写那么多，才学疏浅，如有不当地方还请海涵，感谢指点！]]></content>
      <categories>
        <category>C++之那些年踩过的坑</category>
      </categories>
      <tags>
        <tag>C++</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[C++之那些年踩过的坑（三）]]></title>
    <url>%2F2017%2F05%2F28%2Fp1705281%2F</url>
    <content type="text"><![CDATA[注：本文来自于我的一篇博客 http://www.cnblogs.com/GodA/p/6569254.html ，修改而成。 这一篇会讲一个很小的点，但又经常容易犯错 ¯_(ツ)_/¯ ———— unsigned type。 一、unsigned type 的坑看到这篇的开头，你可能就会想，unsigned type 能有什么坑的呀！那我们就直接了当一些吧！ 1. 小心可能陷入的死循环其实单独的 unsigned type 你还是比较容易想明白的，怕的就是它跟一些其它东西搭配（比如 auto），而你又忽略了那就是 unsigned type 的时候，例如：123456789101112#include &lt;iostream&gt;#include &lt;cstring&gt;int main()&#123; char sz[] = "Hello world!"; // 倒序输出字符串 for (auto i = std::strlen(sz) - 1; i &gt;= 0; --i) &#123; std::cout &lt;&lt; sz[i]; &#125;&#125; 对于上述的代码，会有什么问题吗？当然啦，有些经验的人还是一眼就能看出来的，还有一些人需要多看两眼才能看出来。 问题就在于 std::strlen 的返回值类型是 std::size_t ，这是一个 unsigned type，而一个 unsigned type 的数值永远大于等于 0，是的没错，你很快就反应过来，死循环了。当然这个例子比较简单，而且如果你用 VS 这种有 intelliSense 的 IDE，你敲出函数，或者鼠标移到函数名字上，就能看到函数的声明。那万一不是用这种 IDE 的呢？又或者，一些你不认识的函数，而且类型也被 typedef 到你认不出来了，你又如何分辨呢？虽然这是简单的例子，但确实存在这种风险。 2. 小心可能的访问越界其实上面的例子也是属于访问越界，但 C++ 的数组兼容 C，而 C 的数组是不作边界检查的，所以实际上，上述代码运行起来不会崩溃，甚至还不一定会出错。为什么说不一定呢，因为 sz[-1] 的内容是不知道的…所以输出也有可能让你撞对了，但是！这个代码一定是错误的。所以我会推荐使用 C++ 的容器，对于日常使用，已经绰绰有余了。C++ 的容器，在 Debug 模式下，是会做边界检查的。比如用 VS，在 Debug 下，容器访问越界了，它会有弹出一个 MessageBox 显示 xxx subscript out of range 之类的信息。 好了回归正题，使用容器时，要是一不小心，写出了类似代码：12345std::vector&lt;int&gt; v&#123; 1,2,3,4,5 &#125;;for (auto i = v.size() - 1; i &gt;= 0; --i)&#123; std::cout &lt;&lt; v[i] &lt;&lt; " ";&#125; 就会导致了访问越界了，原因也是 size() 的返回类型是 std::size_t 。 之前我写一个 BigInteger 类的时候，就有很多用到了这样的倒序输出的地方。一开始没注意，也是出现了访问越界的错误，后来看到 vector subscript out of range 才反应过来。 解决方案unsigned type 的坑其实也不算很深，主要在于 0 这个点，要注意以下。解决的方案（至少）有以下几种： 1. 显示指定可容纳范围内的 signed type如果你知道你数据的确切范围，比如不会超过 100w，那么就可以用一个能够放得下 100w 的一个 signed type 去指定它：1for (int32_t i = v.size() - 1; i &gt;= 0; --i) 2. 手动做边界检查如果你不能确定大小，而且不确定 signed type 能否容纳下那个 unsigned type 的全部范围，那就自己在循环的周围做一个边界检查：123456789if (v.size() != 0)&#123; for (auto i = v.size() - 1; i &gt;= 0; --i) &#123; std::cout &lt;&lt; v[i] &lt;&lt; " "; if (i == 0) break; &#125;&#125; 3. 把边界检查移到循环外部其实是第2种方法的变形，如果你唯恐在循环内检查损失了很多性能，那么就可以这样做：12345678if (v.size() != 0)&#123; for (auto i = v.size() - 1; i &gt; 0; --i) &#123; std::cout &lt;&lt; v[i] &lt;&lt; " "; &#125; std::cout &lt;&lt; v[0];&#125; 4. 使用 C++ 迭代器既然用了 C++ 的容器，那么更好的写法当然是这样啦：1234for (auto i = v.rbegin(); i != v.rend(); ++i)&#123; std::cout &lt;&lt; *i &lt;&lt; " ";&#125; 这样就不需要去考虑下标范围啦，什么检查啦，多方便！如果想写出更泛型，更有 C++ Style 的代码，还可以这样：1std::for_each(std::rbegin(v), std::rend(v), [&amp;](int i) &#123; std::cout &lt;&lt; i &lt;&lt; " "; &#125;); 最终选择什么样的方式还是看实际情况跟个人爱好咯！ 三、其它杂谈我在网上看到不少关于什么“代码优化技巧”等等文章，即使是最近出的，还是这样写，也不知道是不是到处抄的。比如我看到其中一篇就说到： 有些处理器处理无符号unsigned 整形数的效率远远高于有符号signed整形数 正确与否我就先不说了，错别字我也不去说了。留给读者先自己实验一下，我会在下一篇中用一篇来讨论一下这类问题。 四、总结小标题即总结。]]></content>
      <categories>
        <category>C++之那些年踩过的坑</category>
      </categories>
      <tags>
        <tag>C++</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[C++之那些年踩过的坑(二)]]></title>
    <url>%2F2017%2F05%2F23%2Fp1705231%2F</url>
    <content type="text"><![CDATA[注：本文来自于我的一篇博客 http://www.cnblogs.com/GodA/p/6554591.html ，修改而成。 这一篇将会聊聊C++中一个极具迷惑性的关键字 ———— inline。虽然只是一个小小的关键字，但要是没有真正了解它，也是很容易踩坑的。 一、什么是 inline或许你对它有那么一点点熟悉，但是又说不清。它的中文翻译为“内联”。它经常跟一个东西共同出现，称为“内联函数(inline function)”。正是这样的翻译，对新手产生了太多的误会。那么什么是 inline 呢？现在暂时不做解答。接下来你可能会经常用到 cppreference，所以你可以先点开放到一旁，然后我们进入正题。 二、inline 之初印象上网搜一下，C++ inline 的作用，你看到的都是什么？都是说 inline 函数可以自动把函数展开呀，可以减小函数调用的开销呀……还会好心提醒你，太大的函数不宜用 inline 呀！会导致代码膨胀呀！只有那些短小精悍的、经常调用的函数使用 inline 才能看到非常明显的效果呀…………我在 N 年前，也是这么傻傻的，所以看到短函数就想加 inline，唯恐性能变差。于是，在印象里，inline 就跟优化挂上钩了。 三、inline 之初体验我们先从简单的例子开始，新建一个 main.cpp 源文件：123456789101112#include &lt;iostream&gt;int max(int a, int b)&#123; return a &lt; b ? b : a;&#125;int main()&#123; int res = max(1, 2); std::cout &lt;&lt; res &lt;&lt; "\n";&#125; 我们编译运行，好，通过了。看到这个函数手痒了是吧，好我们给它加上 inline，再次编译运行，也通过了。到这里还没有问题，具体它的优化我们暂时不去分析。我们要养成一个工程习惯嘛！这是个简单的例子，但实际中我们写的代码可能很多，可能有很多很多个类似于 max 这样的函数，于是我们就想着把他们区分开，于是我们又创建了一个 a.h 头文件，把 max 扔进去，我们还想着接口与实现分离，于是我们创建一个 a.cpp 源文件来实现函数的定义，文件结构变成了这样：12// a.hint max(int a, int b); 1234567// a.cpp#include "a.h"int max(int a, int b)&#123; return a &lt; b ? b : a;&#125; 12345678910// main.cpp#include &lt;iostream&gt;#include "a.h"int main()&#123; int res = max(1, 2); std::cout &lt;&lt; res &lt;&lt; "\n";&#125; 我们运行一遍，没有问题，然后我们试着给 max 函数加上 inline，你知道，Ⅰ.inline 要跟函数的定义放在一块，所以我们就要在 a.cpp 给它加上：1inline int max(int a, int b)&#123;...&#125; 我们再次编译运行，WTF！！怎么报错了！！不就加了个 inline 吗？！！ 冷静了一会，我们看看报错信息，如果你用的是 VS，那么你大概会看到这样的提示： 1&gt;main.obj : error LNK2019: 无法解析的外部符号 &quot;int __cdecl max(int,int)&quot; (?max@@YAHHH@Z)，该符号在函数 _main 中被引用 1&gt;C:\Users\Alinshans\documents\visual studio 2017\Projects\test\Debug\test.exe : fatal error LNK1120: 1 个无法解析的外部命令 是不是感觉有点熟悉？似曾相识？如果你用的是 G++ 去编译，那么大概会得到这样的提示： main.cpp:(.text+0x13): undefined reference to `max(int, int)&apos; 要是你对C/C++编译的过程有一点点了解的话，我们继续尝试用 G++ 去分别运行下面的命令：12$ g++ -c main.cpp$ g++ main.o -o a.out 你就会发现，运行第一条命令（编译）时，是没有错的，运行第二条命令（链接）时，就报错了。这里说明一下，大多数的建置环境都是在编译过程进行 inlining（为了替换函数调用，编译器需要知道函数的实体长什么样，这就解释了 Ⅰ），某些可以在连接期完成，少数的可以在运行期完成。我们只考虑绝大部分情况：inlining 在大多数C++程序中是编译期行为。好了，我们讲回来，为什么会出现这个链接错误呢？注意到刚刚打开的网页 这里的第二、三条 ： 2) The definition of an inline function or variable (since C++17) must be present in the translation unit where it is accessed (not necessarily before the point of access).3) An inline function or variable (since C++17) with external linkage (e.g. not declared static) has the following additional properties: 1) It must be declared inline in every translation unit. 2) It has the same address in every translation unit. 这里提到了 external linkage，若想详细了解可以看 这里。嫌太长不看的你只需要知道我们定义的 max 函数，具有 external linkage，那么它就要满足： 在你需要引用它的编译单元可见 在每个编译单元都要声明为 inline 用人话讲就是，你要把这个 inline function 的定义放到每一个你需要引用的 .cpp(.cc/.cxx等) 源文件。也就是说你要把 main.cpp 写成这样：123456789101112131415// main.cpp#include &lt;iostream&gt;#include "a.h"inline int max(int a, int b)&#123; return a &lt; b ? b : a;&#125;int main()&#123; int res = max(1, 2); std::cout &lt;&lt; res &lt;&lt; "\n";&#125; 好了，这下就没问题了，可以编译通过了。但是，这就需要我们把在源文件内声明为 inline 的函数，复制到每一个需要引用的单元，这样可不好看。所以，一般情况下，Ⅱ. inline 函数的定义放在头文件中，而不放在源文件中。 四、inline 之再体验经过刚刚的了解，我们就试一试在头文件中给函数加上或不加上 inline 的区别吧！我们在 a.h 中新增一个函数：1234567// a.hint max(int a, int b);inline int min(int a, int b)&#123; return a &lt; b ? a : b;&#125; 好，a.cpp 文件依然没有变：1234567// a.cpp#include "a.h"inline int max(int a, int b)&#123; return a &lt; b ? b : a;&#125; 在 main.cpp 中引用这个 min 函数：12345678910111213141516// main.cpp#include &lt;iostream&gt;#include "a.h"inline int max(int a, int b)&#123; return a &lt; b ? b : a;&#125;int main()&#123; int res = max(1, 2); int res2 = min(1, 2); std::cout &lt;&lt; res &lt;&lt; " " &lt;&lt; res2 &lt;&lt; "\n";&#125; 好我们编译运行一下，很好通过了，没有什么问题。我们试试看去掉 inline 会有什么区别吧！WTF!!!怎么又报错了！？？这次又是什么鬼！？这一次，VS的提示是： 1&gt;main.obj : error LNK2005: &quot;int __cdecl min(int,int)&quot; (?min@@YAHHH@Z) 已经在 a.obj 中定义 1&gt;C:\Users\Alinshans\documents\visual studio 2017\Projects\test\Debug\test.exe : fatal error LNK1169: 找到一个或多个多重定义的符号 是不是感觉也似曾相识？（注意，若使用G++编译运行时，可能没有报错，并且正常运行，但这不是值得侥幸的）我们稍微思考一下就能想明白：在 a.h 中定义了 min 这个函数，而在 main.cpp 中，又 #include 了 a.h 这个文件，其中 a.cpp 这个编译单元在生成 a.obj 时会为 min 生成一个实体，而 main.cpp 又为 min 生成了一个实体，所以就会出现重定义。那为什么我们使用 inline 时，就不会有这个错误呢？ 五、什么时候应该使用 inline我们还是翻到刚刚的链接，看到 第一条： 1) There may be more than one definition of an inline function or variable (since C++17) in the program as long as each definition appears in a different translation unit and (for non-static inline functions and variables (since C++17)) all definitions are identical. For example, an inline function or an inline variable (since C++17) may be defined in a header file that is #include’d in multiple source files. 用人话说就是声明为 inline 的函数可以在多个编译单元中重复定义，且只会为它们生成一份实体。刚刚我们在 a.h 内定义的函数，就必须要加上 inline 声明（或者static也是可以的，这里不展开了）。所以，Ⅲ. 当且仅当函数定义在头文件且有可能被多个源文件包含时，使用 inline 。 六、inline 与类成员函数、模板这个部分应该也是很让新手纠结的。因为一个类，可能会有很多 getter/setter 之类的短小的函数，于是就会去纠结要不要加 inline。同时类的成员函数定义的位置，也有以下三种（假设类的声明在 a.h，定义在 a.cpp）： 在头文件中，定义在类中 在头文件中，定义在类外 在源文件中 我们一个一个来谈谈。首先是定义在类中的，需不需要加 inline 呢？还是看到刚刚的页面，最上面这里： A function defined entirely inside a class/struct/union definition, whether it’s a member function or a non-member friend function, is implicitly an inline function. 定义在类中的函数，是隐式 inline 的，所以不需要你加 inline，而且，LLVM CodingStandards 也是这样提出的： Don’t use inline when defining a function in a class definition 我们再讲讲定义在源文件中的，要是你认真看过本文，你就会知道，前面说了，不要把 inline 函数定义在源文件中，所以如果你类成员函数定义在了源文件中，也不需要加 inline。剩下一个，其实我想不到什么理由，可以让成员函数既不定义在类内，也不定义在源文件中（模板类成员函数/类模板成员函数除外）。如果成员函数比较短小，那么你就可以直接定义在类内，否则可以定义在源文件中。而要是非得要定义在头文件且在类外，那就必须要声明为 inline，否则也会有一个重定义的错误。但为什么不直接定义在类中呢？所以综上所述，Ⅳ. 不要把 inline用于类成员函数。 然后再讲模板，包括了函数模板、类模板成员函数和模板类成员函数。有点晕是吧，反正就是有带模板的函数。这些函数具有 inline 语义，是自带 inline 属性的。也就是说，你把刚刚的那个 a.h 文件里的 min 函数改成模板：12345template &lt;typename T&gt;T min(T a, T b)&#123; return a &lt; b ? a : b;&#125; 不需要加上 inline，也是可以正常运行的。所以，Ⅴ. 模板不需要声明为 inline，也具有 inline 的语义。 七、inline 与优化刚刚说了 inline 的用法，现在终于到了要摧毁印象的时候了。我们就先用 main.cpp 来测试：1234567891011121314// main.cpp#include &lt;iostream&gt;#include &lt;cstdio&gt;inline int max(int a, int b)&#123; return a &lt; b ? b : a;&#125;int main()&#123; int res = max(1, 2); std::printf("%d\n", res);&#125; 现在，max 函数是声明为 inline 的，我们可以看反汇编代码，来看看 max 是否有调用。使用 GCC，可以分别运行以下三条命令：123$ g++ -E main.cpp -o main.i$ g++ -S main.i -o main.s$ g++ -O2 -S main.i -o main2.s 然后 main.s 和 main2.s 就是分别未使用优化和 使用了O2优化后的反汇编代码。在 VS 下看反汇编就非常简单了，随便设置一个断点，然后点调试-&gt;开始调试，等程序运行后，点调试-&gt;窗口-&gt;反汇编，就可以看到反汇编代码了。因为 VS 的反汇编的代码比较清晰好看，所以就以 VS 中的反汇编为例。我们先在 Debug 模式下，查看反汇编代码（主要部分）：123456789101112 int res = max(1, 2);002218AE push 2 002218B0 push 1 002218B2 call max (02212BCh) 002218B7 add esp,8 002218BA mov dword ptr [res],eax std::printf("%d\n", res);002218BD mov eax,dword ptr [res] 002218C0 push eax 002218C1 push offset string "%d\n" (0227B30h) 002218C6 call _printf (022132Fh) 002218CB add esp,8 我们可以看到，是有调用 max 函数的。我们再切换到 Release 模式，查看反汇编代码：12345678 int res = max(1, 2); std::printf("%d\n", res);00131040 push 2 00131042 push offset string "%d\n" (01320F8h) int res = max(1, 2); std::printf("%d\n", res);00131047 call printf (0131010h) 0013104C add esp,8 是的，max 函数的调用已经不见了，不过你认为这是拜你加上的 inline 所赐的吗？我们去掉 inline ，再重复一遍刚刚的过程，你会发现，结果是一模一样的。你没有死心，说，这个函数太简单了，我是编译器我也能看得出来怎么优化，要是函数复杂一点，比如有循环、递归什么的，编译器就不会自动优化了！那好吧，我们把 main.cpp 改成这样：1234567891011121314151617#include &lt;iostream&gt;#include &lt;cstdio&gt;int test(int i)&#123; int x = 0; for (int j = 0; j &lt; i; ++j) &#123; x += j; &#125; return x;&#125;int main()&#123; std::printf("%d\n", test(100));&#125; 在 Debug 下反汇编：12345678 std::printf("%d\n", test(100));010118AE push 64h 010118B0 call test (0101136Bh) 010118B5 add esp,4 010118B8 push eax 010118B9 push offset string "%d\n" (01017B30h) 010118BE call _printf (0101132Fh) 010118C3 add esp,8 在 Release 下反汇编：12345678910111213141516171819202122232425 std::printf("%d\n", test(100));00F31042 xor ecx,ecx 00F31044 xor eax,eax std::printf("%d\n", test(100));00F31046 xor edx,edx 00F31048 xor esi,esi 00F3104A xor edi,edi 00F3104C nop dword ptr [eax] 00F31050 inc edi 00F31051 add esi,2 00F31054 add edx,3 00F31057 add ecx,eax 00F31059 add edi,eax 00F3105B add esi,eax 00F3105D add edx,eax 00F3105F add eax,4 00F31062 cmp eax,64h 00F31065 jl main+10h (0F31050h) 00F31067 lea eax,[edx+esi] 00F3106A add eax,edi 00F3106C add ecx,eax 00F3106E push ecx 00F3106F push offset string "%d\n" (0F320F8h) 00F31074 call printf (0F31010h) 00F31079 add esp,8 喔，不要看它这么长，其实它是直接算出结果的了，所以已经没有 test 的调用了。这次看用G++生成的反汇编会更清晰一些：不开优化：123456789101112131415161718main:.LFB1022: .cfi_startproc pushq %rbp .cfi_def_cfa_offset 16 .cfi_offset 6, -16 movq %rsp, %rbp .cfi_def_cfa_register 6 movl $100, %edi call _Z4testi movl %eax, %esi movl $.LC0, %edi movl $0, %eax call printf movl $0, %eax popq %rbp .cfi_def_cfa 7, 8 ret 开O2优化：12345678910111213main:.LFB1022: .cfi_startproc subq $8, %rsp .cfi_def_cfa_offset 16 movl $4950, %esi movl $.LC1, %edi xorl %eax, %eax call printf xorl %eax, %eax addq $8, %rsp .cfi_def_cfa_offset 8 ret 看到了吧，这一次的 test 函数，我没有加 inline，在开启编译器优化的情况下，它还是可以自动去优化的。你还会说，那啥，那啥……你还想说什么，自己去验证吧。我可以做最后一个实验。现在把 main.cpp 改成这样：1234567891011121314151617181920212223242526272829303132333435363738// main.cpp#include &lt;iostream&gt;#include &lt;cstdio&gt;#include &lt;cmath&gt;inline int test(int i)&#123; int prime[100]; int k = 0; for (int n = 2; n &lt;= i; ++n) &#123; bool is_prime = true; for (int j = 2; j &lt;= static_cast&lt;int&gt;(std::sqrt(n)); ++j) &#123; if (n % j == 0) &#123; is_prime = false; break; &#125; &#125; if (is_prime) &#123; prime[k] = n; ++k; &#125; &#125; int sum = 0; for (int n = 0; n &lt; k; ++n) &#123; sum += prime[n]; &#125; return sum;&#125;int main()&#123; std::printf("%d\n", test(100));&#125; 嗯…是有点儿长，我可是把 test 函数声明为 inline 的！然后在 Debug 下反汇编：12345678 std::printf("%d\n", test(100));00131ABE push 64h 00131AC0 call test (013102Dh) 00131AC5 add esp,4 00131AC8 push eax 00131AC9 push offset string "%d\n" (0137B30h) 00131ACE call _printf (0131339h) 00131AD3 add esp,8 在 Release 下反汇编：123456 std::printf("%d\n", test(100));00FC1170 call test (0FC1040h) 00FC1175 push eax 00FC1176 push offset string "%d\n" (0FC20F8h) 00FC117B call printf (0FC1010h) 00FC1180 add esp,8 我声明了 inline，可是无论开不开优化，它也不帮我优化了。 八、inline 的真正意义现在你该停下来思考思考了，什么是 inline？是“内联”吗？inline 的意义是什么？发起一个“内联”请求吗？但事实上，你会发现，有时候，你不用 inline，会报错；有时候，你用了 inline，又会报错。你期望使用 inline 可以优化程序效率，但貌似跟你加不加 inline 没有什么关系啊？inline 的意义，似乎与“内联”，已经渐行渐远了。 好好的思考一下。 我们还是继续翻开我们的 cppreference，注意到这里有一段话： The original intent of the inline keyword was to serve as an indicator to the optimizer that inline substitution of a function is preferred over function call, that is, instead of executing the function call CPU instruction to transfer control to the function body, a copy of the function body is executed without generating the call. This avoids overhead created by the function call (copying the arguments and retrieving the result) but it may result in a larger executable as the code for the function has to be repeated multiple times.Since this meaning of the keyword inline is non-binding, compilers are free to use inline substitution for any function that’s not marked inline, and are free to generate function calls to any function marked inline. Those optimization choices do not change the rules regarding multiple definitions and shared statics listed above. 看不懂没关系，其实它就是说：在很久很久以前，inline 作为给编译器优化的提示符，而 inline 的含义是非绑定的，编译器可以自由的选择、决定是否 inline 一个函数。而如今，编译器根本不需要这样的提示，如果它认为一个函数值得 inline，它会自动 inline，否则，即使你 inline 了，它也会拒绝。可以看看这一篇 SO上的回答： It is said that inline hints to the compiler that you think the function should be inlined. That may have been true in 1998, but a decade later the compiler needs no such hints. Not to mention humans are usually wrong when it comes to optimizing code, so most compilers flat out ignore the ‘hint’. static - the variable/function name cannot be used in other compilation units. Linker needs to make sure it doesn’t accidentally use a statically defined variable/function from another compilation unit. extern - use this variable/function name in this compilation unit but don’t complain if it isn’t defined. The linker will sort it out and make sure all the code that tried to use some extern symbol has its address. inline - this function will be defined in multiple compilation units, don’t worry about it. The linker needs to make sure all compilation units use a single instance of the variable/function. 现在你应该差不多能够理解了，现在的编译器，并不需要你用 inline 去提醒，不要小看搞编译器那帮人，想着帮编译器优化的，一般人往往是错误的。所以，Ⅵ. 当且仅当你想用 inline 去优化程序时，不要使用 inline。inline 这个关键字的翻译，就是一个坑，它真正的意义并不是去内联一个函数，而是表示 哥，别怕！无论你看到了多少个定义，但实体就我一个！在这个 cppreference 里，最重要的就是这一句话： Because the meaning of the keyword inline for functions came to mean “multiple definitions are permitted” rather than “inlining is preferred”, that meaning was extended to variables. 翻译过来就是：Ⅶ. inline 的含义更多的是“允许多重定义”而不是“优先选择内联”。 最后希望看完这篇文章的童鞋们，都可以深刻的理解C++的inline。 九、总结 Ⅰ.inline 要跟函数的定义放在一块 Ⅱ. inline 函数的定义放在头文件中，而不放在源文件中 Ⅲ. 当且仅当函数定义在头文件且有可能被多个源文件包含时，使用 inline Ⅳ. 不要把 inline用于类成员函数 Ⅴ. 模板不需要声明为 inline，也具有 inline 的语义 Ⅵ. 当且仅当你想用 inline 去优化程序时，不要使用 inline Ⅶ. inline 的含义更多的是“允许多重定义”而不是“优先选择内联” ※注：以上总结适用于不熟悉、不了解 inline 的同学。若对以上内容都了解，使用 inline 的时候，很明白很清楚在做什么，会发生什么，那就随便怎么用啦！有的时候用了，就是为了好看一点呀！(～￣▽￣)～]]></content>
      <categories>
        <category>C++之那些年踩过的坑</category>
      </categories>
      <tags>
        <tag>C++</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[C++之那些年踩过的坑(一)]]></title>
    <url>%2F2017%2F05%2F22%2Fp1705221%2F</url>
    <content type="text"><![CDATA[注：本文来自于我的一篇博客 http://www.cnblogs.com/GodA/p/6501505.html ，修改而成。 这一篇聊聊关于C++中的名称空间（namespace）。 一、避免在全局范围内使用 using namespace xxx注意这个标题中的限定词，避免在全局范围内使用。什么意思呢？比如在头文件中，直接使用 using namespace std 之类的声明，这样是不好的。除非除非，你是最终代码用户，写一个小练习，小测试，否则都不要这么做。之前很喜欢写代码开头就写上 using namespace std，因为可以省很多事，比写什么 cout，endl 方便多了。后来我在写 MyTinySTL 这个项目时，最后写测试，为了方便，就用了 using namespace std，写的时候是挺爽的，一运行就炸了。错误长的惨不忍睹，一大堆的名称冲突。当你使用了 using namespace std 的时候，就把 std 这个名称空间内的内容直接引用到了当前空间，如果跟当前空间的函数、类等重名了，那么编译就不通过了，这个就叫名称污染。比如：1234567891011121314151617181920212223#include &lt;iostream&gt;#include &lt;vector&gt;namespace your&#123;template &lt;typename T&gt;class vector&#123;public: vector() = default; ~vector() = default;private: T* data;&#125;;&#125;using namespace std;using namespace your;int main()&#123; vector&lt;int&gt; v;&#125; 你编译它，就会得到类似不明确的符号 vector 啊，may be std::vector or your::vector 啊之类的。当然了，这太明显了，谁都会看出来。问题就在于，我们不能把问题是否发生，寄托于我们是否能够看出来。当代码多了，文件多了，几十个几百个，上万行十万行，你确定你还记得你在哪个头文件 using 了什么东西？然后在一层一层 include 的情况下，莫名其妙的就炸掉了。所以，还是从根源上杜绝问题比较好。 刚刚说的是 namespace std，但对于其它的 namespace 而言，也是一样的。如果是你自己写的，你都尚未能保证你能避开所有重复的命名，除非你自己写个玩具中的 demo，如果是别人写的，那就更加不可控了。 其实解决办法有很多的，或者说是预防措施，又或者说，其实是一个良好的编程习惯。比起直接在全局使用 using namespace xxx 好一点的做法是：在局部声明并且只声明需要的那个部分。比如如果在某个作用域内经常要使用 std::cout，std::endl 等，你可以这样做：12345678910void test()&#123; using std::cout; using std::endl; cout &lt;&lt; "1+1=" &lt;&lt; 1 + 1 &lt;&lt; endl; cout &lt;&lt; "1+2=" &lt;&lt; 1 + 2 &lt;&lt; endl; cout &lt;&lt; "1+3=" &lt;&lt; 1 + 3 &lt;&lt; endl; // ...&#125; 当然最好的还是在你要使用的东西前显示指定出它归属的名称空间，一来可以更清晰的知道它是来自哪里的库，二来这样写几乎放多久都不会有错。习惯成自然，养成了这个习惯，你就根本不会去想到有 using namespace xxx 这种东西了。 二、Best Practices在 C++ Coding Standards 里，有这样一段话： SummaryNamespace usings are for your convenience, not for you to inflict on others: Never write a using declaration or a using directive before an #include directive.Corollary: In header files, don’t write namespace-level using directives or using declarations; instead, explicitly namespace-qualify all names. (The second rule follows from the first, because headers can never know what other header #includes might appear after them.)DiscussionIn short: You can and should use namespace using declarations and directives liberally in your implementation files after #include directives and feel good about it. Despite repeated assertions to the contrary, namespace using declarations and directives are not evil and they do not defeat the purpose of namespaces. Rather, they are what make namespaces usable. 在 C++ Coding Standards for EECS 381中 P32 有一段话： Follow guidelines for namespace using statements. Namespace declarations and directives. No namespace using declarations or directives are allowed at the top level of a header file. OK if scoped within an inline or member function body or a class declaration. In .cpp files, Place using statements only after all #includes. Prefer using declarations of specific Standard Library functions or classes to using namespacedirectives. Especially in this course, prefer using declarations or directives to explicitly qualifying StandardLibrary names with std::. 在 Google C++ Style Guide 中有一条： You may not use a using-directive to make all names from a namespace available. 看了这几段，就胜过我千言万语了。 三、关于匿名 namespace关于 unnamed namespace，cppreference 如是说到： Unnamed namespaces as well as all namespaces declared directly or indirectly within an unnamed namespace have internal linkage, which means that any name that is declared within an unnamed namespace has internal linkage. 也就是说，现在，在匿名空间里的成员，具有内部链接，跟 static 没啥区别了。不过依然要注意，在同一层次中，可以有多个匿名空间，不过这些匿名空间会被整合成一个，所以不能像这样写，会报重定义：12345678910111213#include &lt;iostream&gt;namespace&#123; void foo() &#123; std::cout &lt;&lt; "1" &lt;&lt; "\n"; &#125;&#125;namespace&#123; void foo() &#123; std::cout &lt;&lt; "2" &lt;&lt; "\n"; &#125;&#125;int main()&#123; foo();&#125; 当然在不同空间内的匿名空间，当然就是不同的啦，下面这样就可以通过了： 1234567891011121314151617#include &lt;iostream&gt;namespace&#123; void foo() &#123; std::cout &lt;&lt; "1" &lt;&lt; "\n"; &#125;&#125;namespace n1&#123;namespace&#123; void foo() &#123; std::cout &lt;&lt; "2" &lt;&lt; "\n"; &#125;&#125;&#125;int main()&#123; foo(); n1::foo();&#125; 在 Google C++ Style Guide 中是这样说 unnamed namesapce 的： When definitions in a .cc file do not need to be referenced outside that file, place them in an unnamed namespace or declare them static. Do not use either of these constructs in .h files.DefinitionAll declarations can be given internal linkage by placing them in unnamed namespaces, and functions and variables can be given internal linkage by declaring them static. This means that anything you’re declaring can’t be accessed from another file. If a different file declares something with the same name, then the two entities are completely independent.DecisionUse of internal linkage in .cc files is encouraged for all code that does not need to be referenced elsewhere. Do not use internal linkage in .h files. 他们鼓励在实现文件中，把那些不需要外部引用的东西放进匿名空间中。陈硕大大在他的 CppPractice 中，第一个提到的就是慎用匿名空间。我觉得，对于他说的不利之处，现在来看，主要还是是第一点。因为匿名namespace里的东西是匿名的，所以万一以后有一天想引用它了，也说不准。其实还是用个具体名称，也不麻烦。对于那些实现细节，或者不希望暴露的，我还是喜欢扔进一个 namespace details {} 或者什么 namespace impl {} 里。 四、总结总结就两点： 不要使用 using namespace xxx，使用 xxx::yyy 使用具名 namespace]]></content>
      <categories>
        <category>C++之那些年踩过的坑</category>
      </categories>
      <tags>
        <tag>C++</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Hello, my world]]></title>
    <url>%2F2017%2F05%2F17%2Fp1705171%2F</url>
    <content type="text"><![CDATA[前言原本在博客园和Github上有写一些随笔和笔记，不过博客园的编辑真的好难看，还不如 Markdown 格式的好看，所以就搭了一个新的博客，会陆续把一些以前写的搬到这里，以及新随笔和笔记都会在这里首发。 Hello, my world大概每个人接触编程后，写的第一个程序都是 Hello world 。我们能轻松的用几行代码，就在一个黑框框或者更漂亮的界面上输出一个 Hello world，多亏了我们是站在了许多巨人的肩膀上。多希望有一天，我可以在自己创造的世界里，说一声 Hello, my world! 启程以上所说的”自己创造的世界”，其实说的是自己创造自己喜欢的环境，可以是一个系统，可以是一种语言，可以是一个框架，甚至可以是一种语法。只要能让自己用得开心，想怎么改就怎么改，想怎么运行就怎么运行。当然，不可能从零开始，也不可能去重写所有的东西。这一个过程应该是自顶向下的，从能最直接接触到的东西开始，有不顺手的地方，那就给它包装，或者重写，直到自己顺手了，开心了为止。]]></content>
      <categories>
        <category>其他</category>
      </categories>
  </entry>
</search>